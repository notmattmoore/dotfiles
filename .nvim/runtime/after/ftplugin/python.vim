" place in .vim/after/ftplugin to get sourced after the filetype plugin loads

" the python plugin sets tabs to be 4 spaces, which I think is too many
setlocal shiftwidth=2                " autoindent width
setlocal tabstop=2                   " tabs count as 2 spaces
setlocal softtabstop=2               " tabs count as 2 spaces
